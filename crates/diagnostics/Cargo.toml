[package]
name = "diagnostics"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
log = { workspace = true, features = ["std"] }
flume = "0.12.0"
humantime = "2"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
