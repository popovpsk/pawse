//! UI data layer: embedded icon assets, bundled theme JSON, and localization
//! string tables. These are resources consumed by the UI but are not UI logic.
//! See `doc.md`.

pub mod assets;
pub mod i18n;
pub mod themes;
