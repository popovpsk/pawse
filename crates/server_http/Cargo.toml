[package]
name = "server_http"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "3"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
